apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentic-operator-sub.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentic-operator-sub.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "agentic-operator-sub.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "agentic-operator-sub.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "agentic-operator-sub.serviceAccountName" . }}
      terminationGracePeriodSeconds: 10
      containers:
        - name: operator
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /manager
          args:
            - --leader-elect={{ .Values.leaderElection }}
            - --health-probe-bind-address=:8082
            - --metrics-bind-address=:{{ .Values.service.metricsPort }}
            - --log-level={{ .Values.logLevel }}
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: LICENSE_SECRET_NAME
              value: {{ include "agentic-operator-sub.licenseName" . | quote }}
            - name: LICENSE_JWT
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentic-operator-sub.licenseName" . }}
                  key: LICENSE_JWT
                  optional: true
            - name: LICENSE_PUBLIC_KEY_B64
              valueFrom:
                secretKeyRef:
                  name: {{ include "agentic-operator-sub.licenseName" . }}
                  key: LICENSE_PUBLIC_KEY_B64
                  optional: true
            {{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ include "agentic-operator-sub.licenseName" . }}
                optional: false
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.service.metricsPort }}
              protocol: TCP
            - name: health
              containerPort: 8082
              protocol: TCP
            - name: webhook
              containerPort: 9443
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: webhook-certs
              mountPath: /tmp/k8s-webhook-server/serving-certs
              readOnly: true
      volumes:
        - name: webhook-certs
          secret:
            secretName: {{ printf "%s-agentic-operator-webhook-tls" .Release.Name | trunc 63 | trimSuffix "-" }}
            optional: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
