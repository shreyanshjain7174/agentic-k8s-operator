replicaCount: 1
image:
  repository: postgres
  tag: "16"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 5432
resources:
  requests:
    cpu: "250m"
    memory: "256Mi"
  limits:
    cpu: "1000m"
    memory: "512Mi"
persistence:
  enabled: true
  size: 8Gi
  storageClass: ""
auth:
  database: agentdb
  username: agentuser
  password: changeme
  postgresPassword: changeme
