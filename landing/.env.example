# Contact form destination (optional).
# If unset, the form falls back to opening a mailto draft.
VITE_CONTACT_FORM_URL=

# Fallback email address for direct contact button + mailto fallback.
VITE_CONTACT_FALLBACK_EMAIL=007ssancheti@gmail.com
